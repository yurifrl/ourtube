OurTube installed!

Access:
  kubectl port-forward svc/{{ include "ourtube.fullname" . }} 3000:3000
  Then open http://localhost:3000

Config: Add channels via ConfigMap at /config/channels.json (or .yaml/.csv)
Storage: Data persisted to PVC
