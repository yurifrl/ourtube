apiVersion: v1
kind: Service
metadata:
  name: {{ include "ourtube.fullname" . }}
  labels:
    {{- include "ourtube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ourtube.selectorLabels" . | nindent 4 }}
